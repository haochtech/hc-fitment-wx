<scroll-view scrollX class="_articles-list-scroll _{{mode}}" wx:if="{{mode==='scroll-order'}}">
    <view class="_articles-list">
        <include src="./articles-item.wxml"></include>
    </view>
</scroll-view>
<view class="_articles-list _{{mode||'column-one'}}" wx:else>
    <include src="./articles-item.wxml"></include>
</view>
